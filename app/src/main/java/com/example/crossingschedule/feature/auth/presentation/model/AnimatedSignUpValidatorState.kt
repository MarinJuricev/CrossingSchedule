package com.example.crossingschedule.feature.auth.presentation.model

enum class AnimatedSignUpValidatorState {
    NO_ERROR,
    USERNAME_ERROR,
    EMAIL_ERROR,
    PASSWORD_ERROR,
    CONFIRM_PASSWORD_ERROR
}